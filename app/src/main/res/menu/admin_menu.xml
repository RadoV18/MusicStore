<?xml version="1.0" encoding="utf-8"?>
<menu xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    tools:showIn="navigation_view">
    <item android:title="Opciones">
        <menu>
            <item android:id="@+id/nav_log_out"
                android:icon="@drawable/ic_log_out"
                android:title="Cerrar Sesión" />
        </menu>
    </item>
    <item android:title="Instrumentos">
        <menu>
            <item android:id="@+id/nav_product_list"
                android:icon="@drawable/ic_list"
                android:title="Lista de Instrumentos" />
            <item android:id="@+id/nav_product_add"
                android:icon="@drawable/ic_add"
                android:title="Agregar" />
            <item android:id="@+id/nav_product_modify"
                android:icon="@drawable/ic_update"
                android:title="Modificar" />
            <item android:id="@+id/nav_product_delete"
                android:icon="@drawable/ic_delete"
                android:title="Eliminar" />
        </menu>
    </item>

</menu>